import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'rick-y-morty',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
